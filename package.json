{
  "name": "okeder",
  "private": true,
  "workspaces": ["web", "shared"],
  "scripts": {
    "dev:web": "npm -w web run dev",
    "build:web": "npm -w web run build",
    "lint": "npm -w web run lint"
  }
}
